for i in range (10,0,-2):#ranges doesnt include last value same as slicing
                    #if there is no start value then by defalut it will start with 0
                    #we can also use step count
                    # also we can use - sign as step cout just we have to change the start and end value  
    print("i is now {}".format(i))